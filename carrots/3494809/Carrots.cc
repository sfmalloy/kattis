#include <iostream>

int
main(int argc, char* argv[])
{
    int n, p;
    std::cin >> n >> p;
    for (int i = 0; i < n; ++i)
    {
        std::string temp;
        std::cin >> temp;
    }
    printf("%d\n", p);

    return 0;
}